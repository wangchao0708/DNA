# Configuration Templates

These JSON files are lightweight reference templates for the released scripts.

- `family_sd1_example.json`: SD1-family attribution settings.
- `evaluate_sd1_example.json`: SD1 example evaluation settings.

The scripts currently use Python constants for configuration. Copy the relevant values from these templates into `Family_*.py` or `Evaluate_Accuracy.py` before running a full experiment.
